# Vendored @repodoctor/contracts

Published snapshot of `@repodoctor/contracts` so this repository installs,
tests, and builds Docker images without checking out a sibling private repo.

Refresh from the workspace root after changing contracts:

```bash
./scripts/sync-contracts-vendor.sh
```
